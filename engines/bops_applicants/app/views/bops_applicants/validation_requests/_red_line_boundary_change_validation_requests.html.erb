<% if @validation_requests[:red_line_boundary_change].present? %>
  <li id="red-line-boundary-change-validation-requests">
    <h3 class="app-task-list__section">
      Confirm changes to the red line boundary
    </h3>
    <ul class="govuk-task-list">
      <% @validation_requests[:red_line_boundary_change].each do |validation_request| %>
        <li class="govuk-task-list__item govuk-task-list__item--with-link">
          <div class="govuk-task-list__name-and-hint">
            <% if validation_request.open? %>
              <%= govuk_link_to "Check red line boundary", edit_red_line_boundary_change_validation_request_path(validation_request.id, access_control_params) %>
            <% else %>
              <%= govuk_link_to "Check red line boundary", red_line_boundary_change_validation_request_path(validation_request.id, access_control_params) %>
            <% end %>
          </div>

          <%= render "validation_request_status", validation_request: validation_request %>
        </li>
      <% end %>
    </ul>
  </li>
<% end %>
