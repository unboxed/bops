---
en:
  activemodel:
    errors:
      models:
        bops_core/notify/email_form:
          attributes:
            body:
              blank: Enter the body of the test message
            email_address:
              blank: Enter an email address for the test message
              invalid: Enter a valid email address for the test message
            subject:
              blank: Enter a subject for the test message
        bops_core/notify/letter_form:
          attributes:
            address:
              blank: Enter an address for the test letter
              too_long: Enter seven lines at most for an address
              too_short: Enter at least three lines for an address
            heading:
              blank: Enter the heading of the test letter
            message:
              blank: Enter the message of the test letter
        bops_core/notify/sms_form:
          attributes:
            body:
              blank: Enter the body of the test message
            phone_number:
              blank: Enter a phone number for the test message
              invalid: Enter a valid phone number for the test message
  bops_core:
    tasks:
      task_submit_buttons:
        save_draft: Save changes
        submit: Save and mark as complete
  shared:
    users:
      form:
        deactivate_user: Deactivate user
        email: Email
        reactivate_user: Reactivate user
        sms: SMS
        submit: Submit
      status_prompt:
        heading: "%{count} users have not confirmed their email"
        message: Resend invites to unconfirmed users.
      table:
        deactivated_at: Deactivated at %{date}
        deactivation_date: Deactivation date
        edit_user: Edit user
        email: Email
        'false': 'No'
        resend_invite: Resend invite
        role: Role
        sms: SMS
        'true': 'Yes'
        two_fa_method: 2FA method
        two_fa_set_up: 2FA set up
        user_details: User details
  tasks:
    update:
      check-and-request-documents:
        failure: Failed to save documents check
        success: Documents check saved
      check-constraints:
        add_constraint:
          failure: Failed to add constraint
          success: Successfully added constraint
        failure: Failed to save constraint review
        remove_constraint:
          failure: Failed to remove constraint
          success: Successfully removed constraint
        save_draft:
          failure: Failed to save draft constraints
          success: Draft constraints successfully saved
        success: Constraints were successfully marked as reviewed
