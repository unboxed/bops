<% content_for :page_title do %>
  <%= t(".add_constraint") %> - <%= t("page_title") %>
<% end %>

<% add_parent_breadcrumb_link "Home", root_path %>

<% add_parent_breadcrumb_link t(".constraints"), constraints_path %>

<% content_for :title, t(".add_constraint") %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-one-quarter">
    <%= render "side_navigation", current: "constraints" %>
  </div>
  <div class="govuk-grid-column-three-quarters">
    <h1><%= t(".add_constraint") %></h1>

    <%= form_with model: @constraint, url: constraints_path, scope: :constraint do |form| %>
      <%= render "form", form: form %>
    <% end %>
  </div>
</div>
