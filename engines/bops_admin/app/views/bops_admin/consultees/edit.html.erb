<% content_for :page_title do %>
  <%= t(".edit_consultee") %> - <%= t("page_title") %>
<% end %>

<% add_parent_breadcrumb_link "Home", root_path %>
<% add_parent_breadcrumb_link "Consultees", consultees_path %>

<% content_for :title, t(".edit_consultee") %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-one-quarter">
    <%= render "side_navigation" %>
  </div>
  <div class="govuk-grid-column-three-quarters">
    <h1>
      <%= t(".edit_consultee") %>
    </h1>

    <%= form_with model: @consultee, url: consultee_path(@consultee), scope: :consultee do |form| %>
      <%= render "form", form:, constraints: @constraints %>
    <% end %>
  </div>
</div>
