<div id="consultee-responses" class="consultee-responses">
  <h2 class="govuk-heading-m">
    <%= t(".heading") %>
  </h2>

  <% responses.reverse_each do |response| %>
    <%= render ConsulteeResponseComponent.new(response: response) %>
  <% end %>
</div>
