<% if planning_application.address_or_boundary_or_constraints_updated? %>
  <%= render(
    partial: "shared/warning_text",
    locals: { message: t(".this_application_has") }
  ) %>
<% end %>
<%= render(
  AccordionSections::FeedbackComponent.new(
    feedback: planning_application.feedback["planning_constraints"],
    warning_message: t(".the_applicant_or")
  )
) %>
<%= render(
  partial: "planning_applications/validation/constraints/info",
  locals: {
    planning_application: planning_application,
    planning_application_constraints: planning_application.planning_application_constraints
  }
) %>
