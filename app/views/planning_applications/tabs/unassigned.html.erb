<%= render "planning_applications/tabs_layout" do %>
  <div class="govuk-tabs__panel" id="unassigned">
    <%= render PlanningApplications::PanelComponent.new(
          planning_applications: @planning_applications,
          type: :unassigned,
          search: @search,
          tab_route: :unassigned_planning_applications_path
        ) %>
  </div>
<% end %>
