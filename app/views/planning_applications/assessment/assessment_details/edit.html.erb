<% content_for :page_title do %>
  <%= t("planning_applications.assessment.assessment_details.#{@assessment_detail.category}.edit") %> - <%= t('page_title') %>
<% end %>

<%= render(
  partial: "shared/assessment_task_breadcrumbs",
  locals: { planning_application: @planning_application  }
) %>

<% content_for :title, t("planning_applications.assessment.assessment_details.#{@assessment_detail.category}.edit") %>

<%= render(
  partial: "shared/proposal_header",
  locals: { heading: t("planning_applications.assessment.assessment_details.#{@assessment_detail.category}.edit")  }
) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render(
      "form",
      planning_application: @planning_application,
      category: @assessment_detail.category,
      rejected_assessment_detail: nil
    ) %>
  </div>
</div>
