<% content_for :page_title do %>
  Edit pre-commencement condition - <%= t("page_title") %>
<% end %>

<%= render(
      partial: "shared/assessment_task_breadcrumbs",
      locals: {planning_application: @planning_application}
    ) %>

<% content_for :title, "Edit pre-commencement condition" %>

<%= render(
      partial: "shared/proposal_header",
      locals: {heading: "Edit pre-commencement condition"}
    ) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render "form", url: planning_application_assessment_pre_commencement_condition_path(@planning_application, @condition) %>
  </div>
</div>
