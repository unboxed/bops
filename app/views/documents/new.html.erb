<% content_for :page_title do %>
  Upload document - <%= t('page_title') %>
<% end %>

<% add_parent_breadcrumb_link "Home", root_path(filter_options: PlanningApplication::FILTER_OPTIONS) %>
<% add_parent_breadcrumb_link "Application", planning_application_path(@planning_application) %>
<% add_parent_breadcrumb_link "Documents", planning_application_documents_path(@planning_application) %>

<% content_for :title, "Upload document" %>

<%= render(
  partial: "shared/proposal_header",
  locals: { heading: "Documents: #{@planning_application.reference}" }
) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-full">
    <h2 class="govuk-heading-m">Upload new document</h2>

    <%= render partial: 'edit_and_upload' %>
  </div>
</div>
