<%- if controller_name != 'sessions' %>
  <%= govuk_link_to "Log in", new_session_path(resource_name), no_visited_state: true %><br>
<% end %>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <%= govuk_link_to "Forgot your password?", new_password_path(resource_name), no_visited_state: true %><br>
<% end %>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <%= govuk_link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name), no_visited_state: true %><br>
<% end %>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= govuk_link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), no_visited_state: true %><br>
  <% end %>
<% end %>
