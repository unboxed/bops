<%= form_with(model: site_visit, local: true) do |form| %>
  <% if site_visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site_visit.errors.count, "error") %> prohibited this site_visit from being saved:</h2>

      <ul>
        <% site_visit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_field :notes %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.text_field :photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
