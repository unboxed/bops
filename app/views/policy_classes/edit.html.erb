<%= render(
  partial: "summary",
  locals: {
    planning_application: @planning_application,
    policy_class: @policy_class
  }
) %>
<%= render(
  partial: "form",
  locals: {
    planning_application: @planning_application,
    policy_class: @policy_class,
    editable: true
  }
) %>
